export class Skill {
  name: string;
  logo: string;
  site: string;

  constructor(nameParam: string, logoParam: string, siteParam: string) {
    this.name = nameParam;
    this.logo = logoParam;
    this.site = siteParam;
  }
}
